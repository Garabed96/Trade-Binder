This isn't a particular order, these were skipped and deprioritized
(Can relist by priority) 

1. card_faces handle card faces 
    - in dB (if needed)
    - implement UI feature for double-sided cards
2. 